[package]
name = "graph-colors"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = { version = "0.9", features = ["small_rng"] }
walkdir = "2"

[profile.release]
opt-level = 3
lto = true
