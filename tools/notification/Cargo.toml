[package]
name = "notification"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
mac-notification-sys = "0.6"
dirs = "6"

[profile.release]
opt-level = "s"
lto = true
strip = true
