## Temporary Files

- Never use system `/tmp` — create a local `tmp/` directory in the project
- Always add `tmp/` to `.gitignore` for personal projects (ask user first for work projects)
- Scripts should output to `tmp/` by default
