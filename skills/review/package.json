{
  "name": "agent-review",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.ts",
    "build": "bun build src/frontend/main.ts --outdir dist --minify",
    "test": "bun test",
    "check": "bunx biome check .",
    "fix": "bunx biome check --write ."
  },
  "dependencies": {
    "snabbdom": "^3.6.2",
    "highlight.js": "^11.11.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest"
  }
}
