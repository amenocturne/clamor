set positional-arguments

default:
    @just --list

# Transcribe audio locally with Whisper
transcribe *FLAGS:
    uv run scripts/transcribe.py "$@"

# Transcribe audio via OpenRouter API
transcribe-api *FLAGS:
    uv run scripts/transcribe_api.py "$@"
