set positional-arguments

default:
    @just --list

# Download YouTube subtitles
subs *FLAGS:
    uv run scripts/yt-subs.py "$@"

# Inject transcript/lyrics into a source note
inject *FLAGS:
    uv run scripts/inject-transcript.py "$@"
