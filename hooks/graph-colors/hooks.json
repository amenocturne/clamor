{
  "Stop": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "bash -c 'graph-colors --exclude=logs,tmp,archive \"$(jq -r .cwd)\"'",
          "timeout": 30
        }
      ]
    }
  ]
}
