description: Personal dev workspace

system_prompt:
  common:
    - git-personal

hooks:
  - worktree

instructions:
  - workspace-routing

settings:
  knowledge_base: /Users/skril/Vault/Obsidian/Mirror
