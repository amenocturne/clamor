## Scala Conventions

- Prefer immutable data structures
- Use for-comprehensions for monadic composition
- Pattern matching over if/else chains
- Cats/ZIO idioms when those libraries are present
- sbt commands: `sbt compile`, `sbt test`, `sbt "testOnly *Spec"`
