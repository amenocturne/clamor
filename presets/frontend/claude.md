## Frontend Conventions

- Components: functional with hooks
- State: prefer local state, lift only when needed
- Styling: follow project convention (CSS modules, Tailwind, etc.)
- Accessibility: semantic HTML, ARIA when needed
